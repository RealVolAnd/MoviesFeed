<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.examples.moviesfeed">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_movies_feed"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_movies_feed_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoviesFeed">
        <activity android:name="com.examples.moviesfeed.ui.home.HomeActivity"
            android:launchMode="singleTask"
            android:noHistory="true">

        </activity>
        <activity android:name="com.examples.moviesfeed.ui.splash.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>